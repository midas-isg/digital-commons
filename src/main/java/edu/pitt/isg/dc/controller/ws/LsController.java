package edu.pitt.isg.dc.controller.ws;

import org.springframework.web.bind.annotation.RestController;


@RestController
public class LsController {
    /*@Autowired
    private NcbiRule rule;

    @RequestMapping(value = "/ls/used-in-entry-as-coverage",
            method = GET,
            produces = {JSON, XML})
    public Object listUsedNcbiAsSpatialCoverageInEntries() {
        return toIds(repo.listSpatialCoverageIds(lsIdentifierSource));
    }*/
}
